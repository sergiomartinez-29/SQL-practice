select count(age) as conteo, age from users where age<24 group by age order by age desc
select max(age)from users group by age
--agrupar filas con valores iguales.

select * from users

-- se muestran agrupaciones de edades máximas